enum BASE_URLS {
    BASE_URL = "https://ecommerce.routemisr.com/api/v1/"
}

enum PRODUCTS_URLS {
    PRODUCTS_URL = "products"
}
enum CART_URLS {
    CART_URL = "carts"
}

enum AUTH_URLS {
    LOGIN_URL = "auth/signin",
    SIGN_UP_URL = "auth/signup"
}
enum CATEGORIES_URLS {
    CATEGORIES_URL = "categories"
}

export {
    BASE_URLS,
    PRODUCTS_URLS,
    CART_URLS,
    AUTH_URLS, CATEGORIES_URLS
}